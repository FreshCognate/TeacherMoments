import React from 'react';

const Create = ({
}) => {
  return (
    <div
      id="scenario-builder"
      style={{ height: 'calc(100vh - 68px', marginTop: '28px' }}
    >
    </div>
  );
};

export default Create;