import React from 'react';
import InviteContainer from '../containers/inviteContainer';

const InviteRoute = ({
}) => {
  return (
    <InviteContainer />
  );
};

export default InviteRoute;

export const handle = { isNavigationVisible: false };