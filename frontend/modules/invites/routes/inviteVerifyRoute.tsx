import React from 'react';
import InviteVerifyContainer from '../containers/inviteVerifyContainer';

const InviteVerifyRoute = ({
}) => {
  return (
    <InviteVerifyContainer />
  );
};

export default InviteVerifyRoute;

export const handle = { isNavigationVisible: false };