import React from 'react';
import SignupVerifyContainer from '../containers/signupVerifyContainer';

const SignupVerifyRoute = () => {
  return (
    <SignupVerifyContainer />
  );
};

export default SignupVerifyRoute;

export const handle = { isNavigationVisible: false };
