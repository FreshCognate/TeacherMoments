import React from 'react';
import ScenarioResponsesContainer from '../containers/scenarioResponsesContainer';

export const handle = { isFullWidth: true };

export default function ScenarioResponsesRoute() {
  return (
    <ScenarioResponsesContainer />
  );
}
