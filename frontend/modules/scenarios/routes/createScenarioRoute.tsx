import React from 'react';
import CreateContainer from '~/modules/create/containers/createContainer';

export const handle = { isFullWidth: true };

export default function CreateScenarioRoute() {
  return <CreateContainer />
}